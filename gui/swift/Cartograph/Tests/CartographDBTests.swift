import XCTest
@testable import Cartograph

final class CartographDBTests: XCTestCase {
    // Tests will be added when we have a test fixture DB
    func testPlaceholder() {
        XCTAssertTrue(true)
    }
}
